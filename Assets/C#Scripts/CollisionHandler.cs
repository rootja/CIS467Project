using UnityEngine;
using System.Collections;

public class CollisionHandler : MonoBehaviour {

//	void OnCollisionEnter2D(Collision2D collider){
//		if (collider.gameObject.tag == "Item") {
//			playerObj.GetComponent<Player>().Inventory.Add (collider.gameObject);
//			Destroy (collider.gameObject);
//			Debug.Log ("The number of items in the player's inventory is: " + playerObj.GetComponent<Player>().Inventory.Count);
//		}
//	}
}
